# deps
node_modules/
.venv/
__pycache__/
*.pyc

# next
.next/
out/
build/
.vercel/
next-env.d.ts
*.tsbuildinfo

# secrets
.env
.env.local
.env*.local

# Seed data derived from fighter.pdf. It contains personal mobile numbers of
# private individuals verbatim, so nothing under data/ is committed. Rerun
# scripts/01_extract.py to regenerate; the pipeline is idempotent.
data/*
!data/.gitkeep

# The source spreadsheet, printed and exported, same reason. places.csv is the
# later export the PDF snapshot is missing the tail of; it carries five private
# mobile numbers verbatim.
fighter.pdf
places.csv

# os
.DS_Store
Thumbs.db

# Supabase CLI local state (config.toml is committed, the rest is not)
supabase/.branches/
supabase/.temp/

# Visual brainstorming companion sessions
.superpowers/
